const value = @import("value.zig");
const domain = @import("domain.zig");

// TODO
pub fn doCallsExn() value.Value {
    return undefined;
}

// TODO
pub fn updateLastMinor(state: *domain.State) void {
    _ = state;
}
